# PRD.md — WordWorld 产品需求文档（v2.2）

> **项目**：WordWorld · 3D 网页沙盒建造游戏（输入英文 → 生成 3D 素材 → 搭建自己的世界）
> **产出日期**：2026-09-19（Week 1 · Day 4）
> **上游依据**：`research.md`（第 2 版，Day 3）——研究回答「做什么 / 为什么做」，本文档回答「做到什么程度 / 怎么算做完」
> **本文档的用途**：Day 5 起的技术选型、写代码、验收，全部以本文档为准。出现「这个要不要做」的争论时，先查第 6 章（功能）、第 10 章（砍掉与后续）和第 12 章（验收标准）。
> **版本说明**：v2.0 按 Day 4「今日实操」要求重组结构，新增开始页面、页面/视图列表、创建场景、数据字段、异常状态、BUG 汇报等板块；验收标准统一集中在第 12 章，逐条编号可检查。v2.1 修复跨章节矛盾。**v2.2 补齐视图规划**：新增 V3 帮助与反馈页，并增加 §6.0.1 功能对照表（谁 · 在哪 · 做什么 · 看到什么）。

---

## 1. 项目概述

### 1.1 一句话定义

**WordWorld** 是一款纯网页的 3D 沙盒建造游戏：玩家**必须用英文描述**才能生成新的建造素材，把生成的素材拖进场景，搭出属于自己的小世界，并导出分享。

### 1.2 要解决的问题

想学英文的人的三个共同困境：

| 困境 | 具体表现 |
|---|---|
| **学了没用处** | 没有非用英文不可的真实场景，学完就搁置 |
| **背了就忘** | 机械记忆（单词书、打卡）留不下长期印象 |
| **不敢写** | 怕写错、没反馈，越不写越不会写 |

**WordWorld 的解法**：把「写英文」变成造东西的**唯一入口**——不是练习题，是工具。你写出的英文会立刻变成眼前的 3D 物体：用得上、看得见、写错了改一改重来就行。

### 1.3 项目边界

| 做 | 不做 |
|---|---|
| 桌面浏览器里的 3D 建造沙盒 | 移动端 App、原生客户端 |
| 用英文描述 → 生成素材变体 | 用中文描述生成 |
| 作品存在本机、导出文件分享 | 账号、云端存储、在线社区 |

### 1.4 关键参数

| 项 | 值 |
|---|---|
| 项目名称 | WordWorld |
| 类型 | 3D 网页沙盒建造游戏 |
| 目标用户 | 全年龄段想学英文的人（Day 4 实操模板建议聚焦大学生，经用户确认**维持全年龄段**；大学生属于核心人群的主要构成） |
| 时间范围 | 28 天（Day 4 起剩余约 25 天） |
| 平台 | 桌面浏览器。**搭建场景**用主流桌面浏览器即可；**英文生成功能**需要浏览器支持 WebGPU（见 E1、AC-6） |
| 运行方式 | 核心体验在本机完成，不依赖账号；算力来源见 §13.2 待定项 |
| 核心循环 | 写英文 → 生成素材 → 摆进世界 → 存档 / 分享 → 想搭更多 → 再写英文 |

---

## 2. 目标与成功标准

### 2.1 产品目标

让「想搭一个东西」和「必须写出英文」变成同一件事。用户不是为了学英文才来，而是为了搭出想要的世界，**在搭建过程中不得不写英文**——学习的动力来自创作欲，不来自课程。

### 2.2 28 天成功标准（可检验）

| # | 成功标准 | 怎么验证 |
|---|---|---|
| S1 | 一个从没用过的人，不看说明就能搭出一个有 5 个以上物体的场景 | 邀请 1 位朋友直接试用，全程不提示，观察能否独立完成 |
| S2 | 完成一次完整核心循环：写英文 → 生成素材 → 拖进场景 → 保存 → 导出分享 | 自己走一遍全流程并录屏，全流程无阻断 |
| S3 | 同一个人愿意主动再来第二次，且第二次生成次数 ≥ 第一次 | 记录试用者的两次使用数据（生成次数、场景物体数） |
| S4 | 作品能被另一台机器打开 | 把导出的 JSON 发到另一台电脑导入，场景还原一致 |
| S5 | 项目本身可交付：代码在仓库、文档齐全、能跑起来 | 仓库有完整提交记录；README 写明如何启动 |

> 说明：S5 提到的 README 属于 **Day 27–28 的收尾文档任务**，不在 P0 六项功能范围内——看到它不用提前去做。

### 2.3 本期不追求什么

- **不追求生成质量**：AI 生成的素材只要「和原型有肉眼可辨的差异」即算达标，不追求震撼效果。
- **不追求性能极致**：能跑、不卡到影响操作即可，不做渲染管线级别的优化。
- **不追求功能齐全**：P0 六项做完就是成功，P1 完全不做也不影响验收。
- **不追求商业模式**：不收费、不打广告、不做增长。

---

## 3. 用户与核心场景

### 3.1 目标用户

| 人群 | 特征 | 在本产品里的诉求 |
|---|---|---|
| **核心人群**：想学英文但讨厌背单词的人 | 有学习意愿，抵触机械记忆；英文能读能写但不敢用 | 想找到一个「不用背、能动手」的场景把英文用起来 |
| 交叉人群：喜欢沙盒建造的玩家 | 熟悉 Minecraft / Roblox，习惯摆东西 | 想要一个上手更简单、能自己造素材的沙盒 |
| 新兴人群：喜欢 AI 生成内容的创作者 | 对「输入文字出东西」感兴趣 | 想在浏览器里低成本玩 AI 生成，不用配环境 |

**共同点**：都愿意「动手试试」，都讨厌复杂的前置步骤（装软件、注册账号、看教程）。

### 3.2 三个核心场景

**场景 A · 第一次打开（关键场景，决定成败）**
用户被"输入英文就能造出东西"吸引点开链接。页面直接就是一片 3D 空地 + 左侧素材库。他试着从素材库拖出一棵树放到地上，成功了，觉得有意思。接着他看到输入框，输入 `a big red house`，几秒后得到一间红色的房子，拖进场景里——**这一刻他理解了玩法**：想要什么，就用英文说出来。

**场景 B · 搭建自己的小世界（主要使用场景）**
用户已经会玩了，想搭一整片场景：草地、小河、木桥、几只动物。他不断写英文生成新素材、摆进场景、调整大小和朝向。整个过程像在搭积木，写英文是绕不过去的一步。

**场景 C · 把作品带走（留存与传播场景）**
搭得差不多了，他点导出，得到一个 JSON 文件和一张截图。截图发到群里，JSON 发给朋友——朋友导入后能打开他搭的完整世界。

### 3.3 核心循环

```
写英文描述  →  生成素材变体  →  拖进 3D 场景  →  保存 / 导出  →  想搭更多  →  再写英文
    ↑                                                                            │
    └────────────────────────── 创作欲驱动循环 ───────────────────────────────────┘
```

> 循环里**唯一不能省的一步是"写英文"**。任何让用户"不用写英文也能创造新东西"的设计，都违反本产品的核心。

---

## 4. 页面与视图（页面 / 视图列表）

### 4.1 视图总览

| 视图 | 名称 | 用户在这里做什么 | 去往哪里 |
|---|---|---|---|
| **V1** | 开始页面 | 决定「开始新世界」还是「继续上次」 | → V2 |
| **V2** | 主编辑页 | 搭建、生成、保存、导出——产品的全部功能都在这里 | ⇄ V3（顶栏「帮助与反馈」） |
| **V3** | 帮助与反馈页 | 卡住时查用法、看能力边界、找到汇报渠道 | → 返回 V2 |

> **三个视图是 demo 的完整配置**：V1 负责「不吓退第一次来的人」，V2 承担全部玩法，V3 负责「卡住了有出口、有 bug 有地方报」。再多的视图（素材详情页、设置页等）**本期不做**——每多一个视图就多一份导航逻辑与测试范围（回补机制见 §10.6）。
> 三个视图的优先级不同：**V1、V2 属 P0**（缺了 MVP 不成立）；**V3 的「最小形态」必做**（反馈渠道 + 汇报格式 + 能力边界 + 返回，见 AC-41 / AC-43），扩展内容属 **P1-5**。

### 4.2 V1 · 开始页面

**页面元素**：游戏名 WordWorld、一句话介绍（英文 `Type in English, build your world.` + 中文副标「用英文描述，搭出你的世界」）、「开始建造」按钮、「继续上次」按钮（有存档时才可用）。

**设计说明**：开始页保持极简——它的唯一职责是**不吓退第一次来的人**，3 秒内让用户明白这是什么并点下去。

**交互功能（用户动作 → 可观察结果）**

| 用户动作 | 可观察结果 |
|---|---|
| 打开产品链接 | 看到开始页面：游戏名、介绍、「开始建造」按钮 |
| 点「开始建造」 | 进入主编辑页，呈现空白场景（只有地面） |
| 点「继续上次」 | 进入主编辑页，上次的存档还原出来 |
| （无存档时）看「继续上次」按钮 | 按钮置灰或隐藏，页面其他功能不受影响、不报错 |

### 4.3 V2 · 主编辑页

**布局**（借 Roblox Studio 的骨架，见 research.md §4.1）：

| 区域 | 内容 |
|---|---|
| 左侧 | 素材库面板：预制素材分类 + 「我的素材」（生成结果） |
| 中央 | 3D 视口（画面主体，最大区域） |
| 右上 | 英文输入区（生成的入口，核心循环的起点） |
| 顶栏 | 保存 / 导出 / 导入 / 帮助与反馈 |

**交互功能（用户动作 → 可观察结果）**

| 用户动作 | 可观察结果 |
|---|---|
| 从素材库按住素材拖到场景 | 物体落地出现在松手位置 |
| 点击场景中的物体 | 出现操作控件（移动 / 旋转 / 缩放手柄） |
| 拖动控件 / 滚轮 | 物体移动、旋转或缩放，实时跟随 |
| 按 Delete 或点删除 | 物体消失，素材库里还能再拖 |
| 在输入框写英文并提交 | 10 秒内生成素材变体，进入「我的素材」 |
| 点「下载模型」（输入区旁） | 开始下载，进度可见；下载完生成功能解锁 |
| 点顶栏「保存」 | 提示已保存；刷新后场景还在 |
| 点顶栏「导出」 | 得到场景 JSON 文件 / 当前视角 PNG 截图 |
| 点顶栏「导入」 | 选择朋友的 JSON，场景还原 |
| 点顶栏「帮助与反馈」 | 切到 V3 帮助与反馈页：使用说明、能力边界说明、Issues 汇报地址、汇报格式、备选渠道 |

> 每条交互的完整验收标准集中在 §12，此处只写「动作 → 结果」的约定。

### 4.4 V3 · 帮助与反馈页

**页面元素**

| 区块 | 内容 | 优先级 |
|---|---|---|
| 反馈渠道 | GitHub Issues 地址 + 备选渠道（把错误详情直接发给开发者，见 §9.1） | **必做**（AC-41） |
| 汇报格式 | §9.3 的四段式：操作步骤 / 期望看到 / 实际看到 / 错误详情 | **必做**（AC-41） |
| 能力边界说明 | 一段大白话：这一版生成的是「变体」（改颜色 / 尺寸 / 形状），**不是凭空造出的新模型**——提前说清，避免用户误解后失望（与 §6.4、§10.1 的补救措施一致） | **必做**（AC-43） |
| 返回主编辑页 | 一个返回入口，回到离开前的场景 | **必做**（AC-43） |
| 三分钟上手 | 核心循环一句话 + 三个关键操作（拖素材 / 写英文 / 保存导出）各一行 | P1-5 |
| 常见问题 | 把 §8 的 E1–E8 用大白话重述一遍「遇到这个该怎么办」 | P1-5 |

**设计说明**：帮助页不承担玩法，只解决两件事——「卡住了能找到出口」和「有 bug 有地方报」。内容全部是静态文字，零开发风险，所以放在 P0 收尾时做最合适。

**交互功能（用户动作 → 可观察结果）**

| 用户动作 | 可观察结果 |
|---|---|
| 在 V2 顶栏点「帮助与反馈」 | 切到帮助页，看到上表的各区块 |
| 阅读能力边界说明 | 明白「输入 `castle` 得到的是预制城堡的变体，不是凭空生成的城堡」 |
| 点 Issues 地址 | 新标签页打开仓库 Issues 页面 |
| 点「返回」 | 回到主编辑页，场景保持离开前的样子（V2 ⇄ V3 是应用内视图切换，不重新加载页面、不丢未保存内容） |

---

## 5. 创建场景（用户完整流程）

### 5.1 从零到作品的完整流程

1. 打开链接 → 开始页面（V1）
2. 点「开始建造」→ 进入空白 3D 场景（V2）
3. 从左侧素材库拖第一个素材落地——第一次「我做到了」
4. 在输入框写英文（如 `a big red house`）→ 生成变体 → 自动进入「我的素材」
5. 重复 3–4，用移动 / 旋转 / 缩放调整每个物体
6. 点「保存」
7. 导出 PNG 发群里、导出 JSON 发朋友

### 5.2 场景状态

| 状态 | 含义 |
|---|---|
| 空场景 | 只有地面，没有任何物体 |
| 已有物体 · 未保存 | 搭建中；刷新 / 关闭会弹出离开确认（见 AC-39） |
| 已保存 | 存档与画面一致；「继续上次」可还原到这个状态 |

---

## 6. 功能需求

### 6.0 优先级说明

- **P0（必须有）**：缺任何一项，MVP 不成立。6 项。
- **P1（可选有）**：时间允许才做，不做不影响验收。5 项。
- 各功能的**验收标准全部集中在第 12 章**，逐条编号（AC-1 起），本章只写「做什么」和「能力边界」。

#### 6.0.1 功能对照表（谁 · 在哪 · 做什么 · 看到什么）

> 这张表把每个功能和「谁在用、挂在哪一页、验收在哪一条」钉在一起——看到任何一个功能，四问都能直接查表回答。

| 功能 | 谁在用（§3.1） | 在哪个视图（§4.1） | 做什么 → 看到什么 | 验收编号 |
|---|---|---|---|---|
| 开始页面 | 第一次来的用户（场景 A） | **V1** | 点「开始建造」→ 进入空白 3D 场景 | AC-1~4 |
| P0-1 3D 基础场景 | 所有用户 | V2 · 中央 3D 视口 | 打开页面 → 3 秒内出现地面与天空 | AC-5~10 |
| P0-2 预制素材库 | 交叉人群：喜欢沙盒建造的玩家 | V2 · 左侧素材面板 | 点类别筛选 → 只看该类素材，每条带英文名 | AC-11~14 |
| P0-3 拖拽放置与变换 | 所有用户 | V2 · 中央 3D 视口 | 拖素材到地面松手 → 物体落在地面、不悬空 | AC-15~19 |
| **P0-4 英文输入 → 生成变体** | **核心人群：想学英文但讨厌背单词的人** | V2 · 右上英文输入区 | 写一句英文提交 → 10 秒内得到变体，进入「我的素材」 | AC-20~27 |
| P0-5 本地存档 | 所有用户（不想丢掉作品） | V2 · 顶栏 | 点保存后刷新 → 场景原样回来 | AC-28~33 |
| P0-6 导出与分享 | 核心人群（场景 C：把作品带走） | V2 · 顶栏 | 点导出 → 得到 JSON + PNG，朋友导入能还原 | AC-34~37 |
| 帮助与反馈页 | 卡住的人、想报 bug 的人 | **V3** | 点顶栏入口 → 看到反馈渠道、汇报格式、能力边界 | AC-41 / AC-43 |

### 6.1 【P0-1】3D 基础场景

打开页面即进入一片可自由观察的 3D 空地，含地面、天空背景与基础光照。

### 6.2 【P0-2】预制素材库

左侧素材面板，提供 30–50 个卡通风格 3D 模型，分类浏览，每个都带英文名。

### 6.3 【P0-3】拖拽放置与变换

从素材库拖进场景自动落地，可移动、旋转、缩放、删除。

### 6.4 【P0-4】英文输入 → 生成素材变体（核心功能）

**做什么**：用户输入英文描述 → 本地 AI 输出一组参数（颜色 / 尺寸 / 形状微调）→ 程序套用到预制模型上生成变体 → 存进「我的素材」。

**能力边界（提前说清，避免期待落空）**：这一版不生成"从未存在过的 3D 模型"。说 `a giant pink castle`，得到的是一座**粉色、放大的预制城堡**；说 `a tiny blue tree`，得到的是一棵**蓝色、缩小的预制树**。变体方向包括颜色、尺寸、以及部分素材的形状微调。

### 6.5 【P0-5】本地存档

把场景和自制素材存在浏览器本地，关掉浏览器还在。**本期只保留 1 个存档**（再次保存即覆盖更新），多存档列为后续功能——先把一条路走通，优先保证"关掉再打开东西还在"。

### 6.6 【P0-6】导出与分享

导出场景 JSON 文件 + 导出当前视角截图 PNG；支持导入他人分享的 JSON。

### 6.7 【P1】可选功能（时间允许才做，不做不影响验收）

| 编号 | 功能 | 说明 |
|---|---|---|
| P1-1 | 模型 360° 查看器 | 选中物体后可绕着它转一圈查看 |
| P1-2 | 昼夜循环 | 一键在白天 / 夜晚之间切换 |
| P1-3 | 英文填写提示模板 | 输入框旁按类别给英文例句，点击可填入 |
| P1-4 | 「复制错误详情」按钮 | 异常发生时一键把错误信息复制成可粘贴的文本（见 §9.2；BUG 汇报的**必做部分**是帮助页里的 Issues 地址与格式说明，见 AC-41） |
| P1-5 | 帮助页扩展内容 | 「三分钟上手」+ 常见问题（把 §8 的 E1–E8 用大白话说一遍怎么办）；帮助页的**最小形态已含在必做范围**（AC-41 / AC-43），本条只是把内容做厚 |

### 6.8 卡住降级方案（时间不够时的最小可交付范围）

若 Day 20 前后评估进度严重落后，按下表**有序**降级，保证"能玩的核心"一定成立：

| 降级顺序 | 舍弃内容 | 保留内容 |
|---|---|---|
| 第 1 步 | 舍弃全部 P1 | P0 六项 |
| 第 2 步 | 舍弃 P0-6（导出分享） | P0-1 ~ P0-5 |
| 第 3 步 | 舍弃 P0-5（本地存档，改为"不保存，刷新即清空"） | P0-1 ~ P0-4 |
| **底线** | —— | 3D 场景 + 素材库（浏览/拖放）+ 一项写入功能（英文生成变体入素材库） |

> 底线以上必须成立；底线以下（即连生成素材都做不到）时，本项目退化为纯预制素材沙盒，**这等于放弃核心价值**，属于失败情形，不得作为降级选项接受。

---

## 7. 数据字段

> 本章只定义**字段和含义**，不写代码；具体用什么技术存储（IndexedDB 等）是 Day 5 技术选型的事。

### 7.1 场景中的物体（每摆一个物体，一条记录）

| 字段 | 含义 | 示例 |
|---|---|---|
| 物体 ID | 唯一标识，区分场景里每一个物体 | obj-0001 |
| 素材 ID | 这个物体用的是哪个素材（指向素材表） | tree-01 |
| 位置 | 场景坐标：左右 / 高度 / 前后 | (3, 0, -2) |
| 旋转 | 绕垂直轴的角度 | 45° |
| 缩放 | 相对素材原始大小的倍数 | 1.5 |

### 7.2 素材（库里的每一个"种类"）

| 字段 | 含义 | 示例 |
|---|---|---|
| 素材 ID | 唯一标识 | tree-01 |
| 英文名 | 界面显示用，也是英文输入的参照词 | Oak Tree |
| 类别 | 自然 / 建筑 / 动物 / 道具 | 自然 |
| 来源 | 预制 / AI 生成 | AI 生成 |
| 变体参数 | 仅生成来源有：颜色、尺寸倍数、形状微调 | 粉色、2 倍 |

### 7.3 生成记录（每提交一次英文，一条记录）

| 字段 | 含义 | 示例 |
|---|---|---|
| 英文原文 | 用户输入的完整描述 | a giant pink castle |
| 提交时间 | 何时提交 | 2026-09-19 17:00 |
| 结果状态 | 成功 / 失败（含失败原因） | 成功 |
| 生成素材 ID | 成功时指向生成的变体 | castle-01-v03 |

### 7.4 存档元数据（存档的"封面信息"）

| 字段 | 含义 | 用途 |
|---|---|---|
| 存档版本号 | 本文档结构的版本 | 导入时判断版本是否匹配 |
| 最后保存时间 | 最近一次点保存的时刻 | 让用户确认存没存上 |
| 物体数量 | 场景里物体总数 | 界面展示 |
| 占用空间估算 | 存档大致大小 | 让用户心里有数 |

### 7.5 导出文件（JSON）必须包含

1. 存档元数据（§7.4 全部字段）
2. 全部物体记录（§7.1 全部字段）
3. **用到的素材定义**（§7.2，尤其是自制素材）——朋友的机器上没有你的生成素材，不带过去导入就会缺东西

---

## 8. 异常状态清单

> 原则：任何异常都不允许白屏、不允许静默吞掉；用户看到的提示一律中文。

| # | 异常 | 触发条件 | 用户看到什么 | 怎么恢复 |
|---|---|---|---|---|
| E1 | 浏览器不支持 WebGPU | 打开页面时检测 | **场景和预制素材照常可用**（能正常搭建）；只有「英文生成」入口显示中文说明，并给出替代路径 | 继续搭建 / 换浏览器 / 走替代路径 |
| E2 | 素材 / 缩略图加载失败 | 某个素材请求失败 | 该条显示占位图，其余素材正常 | 重开素材库 |
| E3 | 生成超时 | 提交后超过 10 秒无结果 | 提示"生成较慢或失败"，**输入框内容保留** | 重试或改写描述 |
| E4 | 生成失败（结果无法解析） | 模型返回内容不合规 | 中文原因说明 + 英文原文保留 | 改写描述重试 |
| E5 | 未下载模型时使用生成 | 基础模式 | 基础模式说明 + 替代路径入口 | 下载模型或走替代路径 |
| E6 | 存档损坏 / 读不出 | 打开时解析失败 | 中文提示 + 「清空重开」选项（不静默吞掉） | 清空重开或排查文件 |
| E7 | 导入文件非法 / 版本不匹配 | 导入时校验失败 | 明确提示哪里不对 | 修正文件或放弃导入 |
| E8 | 浏览器存储空间不足 | 保存时空间不够 | 中文提示 + 清理指引 | 清空间或清空存档 |

**明确不做（列为后续）**：未保存关闭后的自动草稿恢复——本期用「离开确认」（AC-39）代替，自动恢复列入后续功能（§10.6）。

---

## 9. BUG 汇报

### 9.1 汇报渠道

GitHub 仓库的 **Issues** 页面。本项目零服务器、无应用内后端，应用内不做完整的反馈系统。

⚠️ **门槛说明（诚实记录）**：GitHub 账号对普通学习者偏高——目标用户里可能有人没有账号，也不会提 Issue。因此帮助页里同时写明**备选渠道**：把错误详情直接发给开发者（或发在项目群里），由开发者代为转成 Issue。本期不做应用内直接提交（那需要服务器）。

### 9.2 应用内辅助：「复制错误详情」按钮（P1，时间允许才做）

异常发生时（§8 的任一情况），界面上出现「复制错误详情」按钮：点击后，错误时间、发生场景、错误信息被整理成一段完整文本并复制到剪贴板——用户直接粘贴进 Issue 或发给开发者，不用自己描述。

### 9.3 建议的汇报格式（写在帮助页里）

1. **操作步骤**：你做了什么（一步步）
2. **期望看到**：你以为会发生什么
3. **实际看到**：真正发生了什么（可附截图）
4. **错误详情**：用「复制错误详情」按钮粘贴

### 9.4 处理流程

收到 Issue → 确认能否复现 → 定级 → 修复 → 回复关闭。

| 级别 | 定义 | 处理要求 |
|---|---|---|
| A · 阻断 | 核心循环（写英文 → 生成 → 摆放）走不通 | 优先修，当天处理 |
| B · 严重 | 某个功能坏了但有绕行办法 | 当周处理 |
| C · 一般 | 体验问题、文案错误 | 有空处理 |

---

## 10. 砍掉的功能及理由（含后续回补机制）

> 本章回答今天要掌握的问题：**「你在 PRD 里砍掉了什么功能？为什么砍它？」**

### 10.1 主刀一：把「AI 凭空造物」降级为「AI 改参数」

| 项 | 内容 |
|---|---|
| **原本设想** | 输入 `a giant pink castle` → AI 凭空生成一座全新的 3D 城堡，想象力无边界 |
| **本期改为** | AI 只输出"改哪些参数"（颜色 / 尺寸 / 形状微调）→ 程序把参数套到预制模型上 |
| **砍法** | **降级**（不是完全删除，而是做成简版） |

**为什么砍**
1. **技术做不到**：在浏览器里跑"文字 → 3D 模型"，目前没有可用的成熟方案，需要的模型体量远超浏览器能承受的范围。这是硬约束，不是努力能补的。
2. **时间不允许**：28 天里光是把这条路走通就会吃掉全部工期，P0 其余五项全部完不成。
3. **方向不需要**：本项目的核心是"必须写英文"，不是"生成质量"。变体路线 4–7 秒出结果，反馈快，更贴合学习节奏——等一分钟才看到结果，学习的爽感就断了。

**用户损失了什么（诚实记录）**
- 想象力有天花板：说 `castle` 只能得到预制城堡的变体，得不到一座从未存在过的城堡。
- 素材真实种类受限：能造出的东西的总数受预制模型数量限制。

**补救措施**
- 把"探索变体"本身做成乐趣：同一个词反复试能得到不同结果，鼓励用户多写、多试。
- 界面里**明确写出能力边界**，不让用户产生错误期待后失望。
- 预制素材库做足（30–50 个），用基数弥补变体的想象空间。

**后续**：真·「文字生成 3D 模型」列为 Day 20 之后的探索项，**本期不作承诺**。

### 10.2 主刀二：所有需要服务器的功能，一起砍

- **砍掉**：账号系统、云端存储、多人在线协作、在线社区素材广场。
- **为什么砍**：这四项有一个共同前提——**需要一台服务器**。一旦引入服务器，就要配套账号体系、数据合规、运维成本，28 天内做不完，而且会推翻本产品"打开网页即用、作品只属于你"的定位。
- **代价**：作品只能留在本机；分享靠手动导出文件发给朋友；看不到别人的世界，没有社区氛围。
- **后续**：Day 27–28 如有余力，先做"离线素材包分享"（把自己做的素材打包成文件发给朋友）——这条路**不需要服务器**。

### 10.3 主刀三：脚本 / 编程系统，不做

- **砍掉**：类似 Roblox 的脚本系统（用代码控制物体行为）。
- **为什么砍**：本项目的用户是英文学习者，不是程序员。而"用英文描述"**本身就是本项目的编程语言**——再给一套脚本，等于给用户两个学习负担，还会削弱核心循环。
- **代价**：做不出"会自己开门的房子""会巡逻的机器人"这类交互，场景是静态的。

### 10.4 完整砍掉清单

| # | 砍掉的功能 | 砍法 | 理由类型 | 一句话理由 | 放弃它的代价 |
|---|---|---|---|---|---|
| 1 | 真·「文字生成 3D 模型」 | 降级 | 技术 | 浏览器端没有可用的文生 3D 方案 | 新物体只能由预制模型变形而来 |
| 2 | 账号系统 | 删除 | 方向 | 零账号是"打开即用"定位的前提 | 作品无法跨设备同步 |
| 3 | 云端存储 / 服务器 | 删除 | 方向 + 时间 | 引入服务器就要配套账号与运维 | 只能靠文件手动分享 |
| 4 | 多人在线协作 | 删除 | 时间 | 需要服务器，28 天做不完 | 不能和朋友一起搭 |
| 5 | 在线社区 / 素材广场 | 删除 | 时间 + 成本 | 同上，且需要内容审核 | 没有社区氛围与素材流通 |
| 6 | 脚本 / 编程系统 | 删除 | 方向 | 自然语言就是本项目的"编程" | 不能做逻辑交互，场景是静态的 |
| 7 | 移动端 App | 删除 | 技术 | 手机跑不动 WebGPU + 本地模型 | 手机体验不做保证 |
| 8 | 付费 / 订阅 / 广告 | 删除 | 方向 | 学习工具，不是商业项目 | 无（本期也不需要收入） |
| 9 | 成就 / 徽章 / 等级 | 删除 | 方向 | 素材库的积累本身就是激励 | 少了短期反馈钩子，留存可能偏低 |
| 10 | 听写 / 拼写 / 主动回忆模式 | 删除 | 方向 | 上一个项目的玩法，已推翻 | 无（避免做偏方向） |
| 11 | 多语言界面 | 删除 | 时间 | 界面中文 + 输入英文已够用 | 非中文用户无法使用 |
| 12 | 真实物理引擎 | 删除 | 时间 | 只需展示摆放，不做物理模拟 | 物体不会掉落、滚动，只能静态摆放 |
| 13 | 多种游戏模式 | 删除 | 方向 | 只做"建造自己的世界"一种 | 玩法相对单一 |

### 10.5 用一句话回答今天的问题

**砍掉的是"看起来最酷、但会拖垮整个项目"的功能，留下的是"支撑核心循环"的功能。**

- 最痛的一刀是把「AI 凭空造物」降级成「AI 改参数」——因为技术不允许、时间不够，而且核心是"让用户写英文"，不是"生成得多好"。
- 份额最大的一刀是砍掉所有需要服务器的功能——因为它们共同的成本（账号 + 运维 + 合规）会吃掉全部工期，并推翻"打开即用、作品只属于你"的定位。
- 最反直觉的一刀是砍掉脚本系统——因为**"写英文"就是本项目的编程语言**，多给一套等于削弱核心。

三条刀的共同标准只有一句：**凡是让核心循环（写英文 → 造东西）变慢、变浅、变复杂的，一律砍掉。**

### 10.6 后续回补机制

> **声明：本章砍掉的 13 项，全部是「本期（28 天）不做」，不是「永远不做」。** 本节的用途，是让后续补玩法这件事**有规则可依**，而不是靠一时兴起。

#### 10.6.1 回补难度分级

难度定义：
- **低** = 加一个独立模块，不动现有架构
- **中** = 需要新增一层设计（新数据结构或新交互层）
- **高** = 要换技术路线，或引入一套基础设施

**A 组｜本地就能加，不需要服务器**

| # | 项目 | 难度 | 回补时要注意什么 |
|---|---|---|---|
| P1-1~5 | 五项 P1 可选功能 | 低 | **第一顺位**：闸门一满足即可做，已在 §6.7 写明 |
| 9 | 成就 / 徽章 / 等级 | 低 | 先定判断依据（生成次数？物体数量？），避免变成刷量游戏 |
| 11 | 多语言界面 | 低 | 要先把界面文案抽成语言包，**越晚做越麻烦** |
| 10 | 听写 / 拼写 / 主动回忆模式 | 低 | ⚠️ 方向风险：会把注意力从「写英文去创造」拉回「背英文」，需整体重评 |
| 6 | 脚本 / 编程系统 | 中高 | ⚠️ 若做成写代码，等于否定「英文就是本项目的编程语言」；建议做**可视化条件块**，且必须用英文命名 |
| 12 | 真实物理引擎 | 中高 | 引入物理库会带来性能风险，加之前先测 §12 里「50 个物体」那条帧率标准是否还站得住 |
| 13 | 多种游戏模式 | 中高 | 每种模式等于一条新产品线，别把它当"加个功能" |
| 1 | 真·文字生成 3D 模型 | 高 | **不是工期问题**，是等浏览器端方案成熟——必须换技术方案才可能做 |
| 7 | 移动端 App | 高 | 需重做交互适配 + 解决手机端算力，建议**另立项目**，不要塞进本项目 |
| 8 | 付费 / 广告 | 低 | 技术上容易，但一涉及收款就有合规要求，且本项目定位是学习工具 |

**B 组｜需要一台服务器（四项必须一起考虑）**

| # | 项目 | 难度 | 回补时要注意什么 |
|---|---|---|---|
| 2 / 3 / 4 / 5 | 账号系统 / 云端存储 / 多人在线协作 / 在线社区 | 高 | 它们共同的前提是**服务器**。引入即需同时配齐：账号体系 + 数据存储 + 运维 + 数据合规。**成本量级与 A 组不在一个层面**，应视为「要不要开一个 v2」，不是「加不加功能」 |

#### 10.6.2 回补闸门（四条，全部满足才能动手）

| 闸门 | 条件 | 为什么要有这一条 |
|---|---|---|
| **① P0 全绿** | §12 里 P0 六项的所有验收标准都已打勾 | 核心还没跑通就加料，等于在没打地基时加盖楼层 |
| **② 有外部验证** | 至少 1 位外部测试者不看说明走通完整循环（对应成功标准 S1 / S2） | 确认核心玩法真的成立，再加的都是"真需要"的 |
| **③ 过核心检验** | 通过 §10.5 那句标准：不让核心循环（写英文 → 造东西）变慢、变浅、变复杂 | 防止功能越加越多、英语反而越写越少 |
| **④ 一次一项** | 一次只回补一项；补完**必须重跑一遍 P0 六项验收** | 新功能弄坏老功能是最常见的翻车方式 |

#### 10.6.3 回补时要做完的四件事

1. 在 §10.4 的清单里，把该项状态由「砍掉」改为「**已回补（日期）**」——保持文档与事实一致
2. 在 §12 里**补写它的验收标准**（照现有格式：做某个动作 → 看到某个结果）
3. 在 §13.3 变更记录里加一行，写清改了什么、为什么改
4. 重跑 P0 六项验收，确认没有把已有功能弄坏

#### 10.6.4 本期（28 天内）的建议节奏

| 时点 | 允许做什么 |
|---|---|
| 现在 ~ Day 19 | **一项都不加**。只做 P0，把地基打牢 |
| Day 20 检查点 | 若闸门①②已满足 → 启动 P1 五项（最低风险的回补） |
| Day 21~24 | P1 做完后仍有富余 → 可考虑 A 组「低难度」项，一次一项 |
| Day 25 之后 | **冻结功能**，只修 bug、补文档、做测试 |
| 项目结束（v1.0 之后） | 按 A 组 → B 组的顺序规划 v1.1 / v2 |

> ⚠️ 最需要克制的时刻，是"P0 快做完、感觉自己很行"的时候。那一刻最容易一口气加三四个功能，然后全部做完不了。

---

## 11. 风险与对策

| # | 风险 | 影响 | 对策 | 触发时的动作 |
|---|---|---|---|---|
| R1 | **AI 生成质量不稳定** | 首次体验劝退，用户觉得"就这？" | 用变体路线保证"必定有结果"；界面提前写清能力边界 | 若实测变体差异肉眼不可辨 → 改为"AI 只调颜色"，同时扩充预制素材数量 |
| R2 | **首次要下载模型（约 1–2GB）** | 慢网 / 低配设备的用户直接流失（本项目只做桌面端，不含手机） | 模型**可选下载**；不下载也能用预制素材完整玩一遍；备用替代算力路径；明确告知模型可随时自行删除以释放空间 | 若替代算力路径全部不可行 → 退为"纯素材库也能完整玩一遍"，AI 生成标注为需要下载 |
| R3 | **学习效果未经验证** | 做完了不知道有没有价值 | 从 Day 6 起用一个能度量的轻指标：**人均生成次数** + 素材库增长速度 | 若用户只拖不写（生成次数极低）→ 说明"必须写英文"的力度不够，把生成入口的权重调高 |
| R4 | **技术栈复杂 + 工期紧** | 28 天做不完，项目烂尾 | 严格 P0 / P1 分级；提前写好降级路径 | 按 §6.8 卡住降级方案**有序**舍弃，底线不可突破 |

---

## 12. 验收标准（逐条检查）

### 12.0 验收规则

- **验收环境基准**：所有涉及速度、帧率的标准，都在**同一台基准设备**上测量——即本项目的开发电脑（Day 5 技术选型时记录具体型号与配置）。换设备后性能类标准需要重新标定，不算产品缺陷。
- **不放行模糊说法**：验收标准里不允许出现"目标机型""速度正常"这类当场无法判断的表述——发现一个改一个。
- **格式约定**：每条都是「做某个动作 → 看到某个结果」，你可以亲手试出来，不接受口头结论。
- **⏳ 标记的条目**：标了 ⏳ 的条目（当前只有 AC-26）因为依赖 Day 5 的待定项 D2，**在 D2 定稿前不计入验收**——不计入不等于可以忘，D2 定稿时必须回填。

### 12.1 开始页面（V1）

- [ ] **AC-1** 打开产品链接，先看到开始页面：游戏名、一句话介绍、「开始建造」按钮同时可见
- [ ] **AC-2** 点「开始建造」→ 进入主编辑页，场景为空白（只有地面，无任何物体）
- [ ] **AC-3** 有存档时「继续上次」可点；点击后进入主编辑页，存档还原
- [ ] **AC-4** 无存档时「继续上次」置灰或隐藏，不报错，页面其他功能不受影响

### 12.2 3D 基础场景（P0-1）

- [ ] **AC-5** 基准设备上，桌面版 Chrome 打开主编辑页，3 秒内出现 3D 场景，无白屏、无红色报错
- [ ] **AC-6** 用**不支持 WebGPU 的浏览器**打开 → 仍能进入场景、用预制素材搭建；只有生成入口显示中文说明（含替代路径），全程不白屏、不报错
- [ ] **AC-7** 场景里有明显可见的地面（有网格或纹理，能一眼判断"地面在哪"）
- [ ] **AC-8** 按住鼠标左键拖动可旋转视角，滚轮可拉近拉远
- [ ] **AC-9** 视角不会穿到地面以下（不会出现"从地底往上看"的画面）
- [ ] **AC-10** 画面帧率保持 ≥ 30 FPS（浏览器开发者工具性能面板可查）

### 12.3 预制素材库（P0-2）

- [ ] **AC-11** 面板列出全部预制素材，总数 ≥ 30，每条有名称 + 缩略图
- [ ] **AC-12** 素材按类别分组（自然 / 建筑 / 动物 / 道具），点击类别能筛选
- [ ] **AC-13** 每个素材旁边能看到它的**英文名**
- [ ] **AC-14** 单个素材加载失败时显示占位图，不出现空白或整页崩溃

### 12.4 拖拽放置与变换（P0-3）

- [ ] **AC-15** 从素材库按住拖到场景松手，物体出现在松手位置，且**底面贴合地面**（不悬空、不陷入地下）
- [ ] **AC-16** 选中物体后出现操作控件，可沿至少两个水平方向移动
- [ ] **AC-17** 可绕垂直轴旋转（0–360°）、可等比缩放（0.5–3 倍）
- [ ] **AC-18** 按 Delete 键或点删除按钮，物体消失，且能重新从素材库拖一个进来
- [ ] **AC-19** 场景中同时存在 ≥ 50 个物体时，拖动操作仍然不卡（帧率 ≥ 20 FPS）

### 12.5 英文输入 → 生成变体（P0-4，核心功能）

- [ ] **AC-20** 输入框只接受**英文**；输入中文时给出中文提示并阻止提交，提示里说明"请用英文描述"
- [ ] **AC-21** 英文输入少于 2 个单词时不提交，并提示需要更完整的描述
- [ ] **AC-22** 提交后 **10 秒内**出结果（基准设备上，且不含首次模型下载的等待时间）
- [ ] **AC-23** 生成的变体在**颜色或尺寸**上与原型素材存在肉眼可辨的差异
- [ ] **AC-24** 生成结果自动进入「我的素材」，可像预制素材一样拖进场景使用
- [ ] **AC-25** **不下载模型时**：输入框显示当前为"基础模式"，并给出替代路径的入口说明；不报错、不白屏
- [ ] **AC-26**（Day 5 已回填）"替代算力路径可用"的验证步骤：① 未下载模型时，输入词典可识别的英文（如 `a red tree`），10 秒内返回变体，且数据未离开本机（断网仍可用）② 输入词典无法识别的描述，给出"基础模式"说明 + "可选云端生成"入口，不报错不白屏 ③ 点击"云端生成"时弹出隐私提示（描述将发送云端），用户确认后才发起，结果进入"我的素材"
- [ ] **AC-27** 生成失败时显示中文原因说明，并**保留用户已输入的内容**（不丢字）

### 12.6 本地存档（P0-5）

- [ ] **AC-28** 点「保存」后刷新页面，场景中物体的**数量、位置、旋转、缩放**全部还原
- [ ] **AC-29** 再次点「保存」是**覆盖更新同一存档**，不会产生第二个存档；按钮旁有明确文字说明
- [ ] **AC-30** 完全关闭浏览器再打开，存档仍在
- [ ] **AC-31** 自己生成的变体，重开后仍在，且能继续使用
- [ ] **AC-32** 页面能看到当前存档占用的空间大小
- [ ] **AC-33** 提供「清空存档」按钮，需二次确认后才执行

### 12.7 导出与分享（P0-6）

- [ ] **AC-34** 导出 JSON：文件包含存档元数据 + 全部物体记录 + **用到的素材定义**（含自制素材）
- [ ] **AC-35** 导入 JSON：导入后场景还原到导出时的样子；**在另一台机器上导入同样成立，自制素材也能正常显示**
- [ ] **AC-36** 导出 PNG：得到一张当前视角截图，**画面里不含编辑界面**（只有场景本身）
- [ ] **AC-37** 导入的 JSON 版本不匹配或文件非法时，给出明确提示，不崩溃

### 12.8 主编辑页整体（V2）

- [ ] **AC-38** 顶栏能一眼找到四个入口：保存、导出、导入、帮助与反馈
- [ ] **AC-39** 刷新或关闭页面、且存在未保存改动时，浏览器弹出离开确认（用浏览器原生确认即可，不自绘弹窗）

### 12.9 异常状态（对应 §8 清单）

- [ ] **AC-40** 异常演练：按 §8 的表逐条制造 E1–E8 的触发条件，每条的「用户看到什么」与文档一致，全程无白屏

### 12.10 帮助与反馈页（V3）与 BUG 汇报（对应 §9）

- [ ] **AC-41** 「帮助与反馈」入口里能看到 Issues 汇报地址 + 汇报格式说明 + 备选渠道（发给开发者）
- [ ] **AC-43** 帮助页可从顶栏进入、能返回主编辑页（返回后场景保持离开前的样子）；页面里能看到**能力边界说明**，且英文输入区旁也有一行同样的短句提示
- [ ] **AC-44**（P1-5）帮助页含「三分钟上手」+ 常见问题（按 §8 的 E1–E8 逐条说明遇到时怎么办），无未完成的占位文字

> 「复制错误详情」按钮属 P1（§6.7 P1-4），验收见 §12.11 的 AC-42。

### 12.11 P1 可选功能的验收（做了才算数，不做不影响验收）

| 编号 | 功能 | 验收标准 |
|---|---|---|
| P1-1 | 模型 360° 查看器 | 选中物体后可绕着它转一圈查看，不影响场景其他物体的显示 |
| P1-2 | 昼夜循环 | 一键在白天 / 夜晚之间切换，夜晚时光照变化明显但不全黑（仍能看清物体） |
| P1-3 | 英文填写提示模板 | 输入框旁按类别给出英文例句（如 `a big blue house`），点击例句可填入输入框 |
| P1-4 | 「复制错误详情」按钮 | **AC-42**：异常发生时可点该按钮，粘贴出来是完整可读的文本（含时间、发生场景、错误信息） |
| P1-5 | 帮助页扩展内容（三分钟上手 + 常见问题） | **AC-44**：内容按 §8 的 E1–E8 逐条给出「遇到这个怎么办」，无占位文字 |

---

## 13. 附录

### 13.1 与 research.md 的关系

| | research.md（Day 3） | PRD.md（Day 4，本文档） |
|---|---|---|
| 回答什么 | 做什么、为什么做、别人怎么做的 | 做到什么程度、怎么算做完、不做什么 |
| 用法 | 方向类问题的依据 | 开发与验收的依据 |

**冲突处理**：具体功能细节冲突时以本文档为准（更新时间更晚）；**方向性**冲突（比如要不要坚持纯本地）必须回到 research.md 复看再决定。

### 13.2 待定项（Day 5 技术选型时决定，本文档暂不写死）

| # | 待定项 | 为什么留到 Day 5 |
|---|---|---|
| D1 | 本地 AI 模型的具体选型与体量（语言模型 / 图像模型） | 需要实测机型与下载时间才能定 |
| D2 | **不下载模型时的替代算力路径**的具体形态 | ✅ Day 5 已定稿：本地词典为主 + 云端 API 可选（混合）；AC-26 已回填 |
| D3 | 3D 渲染库与前端框架的具体版本 | 属技术选型范围 |
| D4 | 预制 3D 素材的来源与使用授权（自制 / 免版权素材库） | 需要先确认授权条款 |

> 说明：D2 已定稿（本地词典为主 + 云端 API 可选）。默认数据不出本机；仅在用户主动选择云端算力时数据才出本机，且需用户二次确认。

### 13.3 变更记录

| 日期 | 版本 | 变更内容 | 原因 |
|---|---|---|---|
| 2026-09-19 | v1.0 | 首版 PRD：6 章 + 附录；P0 六项 / P1 三项；砍掉清单 13 项 | Week 1 · Day 4 任务 |
| 2026-09-19 | v1.0 | AI 生成能力定为"先降级，有余力再试真生成" | 用户 Day 4 拍板 |
| 2026-09-19 | v1.0 | 模型下载定为"可选下载 + 替代算力路径（形态待定）" | 用户 Day 4 拍板 |
| 2026-09-19 | v1.0 | P1 三项写入本文档并标注"时间允许才做" | 用户 Day 4 拍板 |
| 2026-09-19 | v1.1 | 新增「后续回补机制」：回补难度分级（A 组本地可加 / B 组需服务器）+ 四道回补闸门 + 回补流程 + 本期节奏 | 用户要求：demo 保持精简，砍掉的玩法后续可逐步补全 |
| 2026-09-19 | v1.2 | AI 自检修复 8 处「必须修改」级问题：① 新增"验收环境基准"，消除「目标机型」这类无法当场判断的说法 ② 补「不支持 WebGPU 时的验收项」 ③ 存档明确为单存档并补验收入口 ④ "替代路径可用"的承诺与验收标准对齐（标记待 Day 5 补全） ⑤ R2 中「手机用户」与"只做桌面端"的矛盾 ⑥ S3 术语「Trial 用户」改为「试用者」 ⑦ S5 的 README 明确归属 Day 27–28 ⑧ 修正剩余天数与 research.md 口径一致 | Day 4 板块③ AI 自检 |
| 2026-09-19 | v2.0 | 按 Day 4「今日实操」要求重组结构：新增 §1.2 要解决的问题、§4 页面与视图（含开始页面 + 页面/视图列表）、§5 创建场景、§7 数据字段、§8 异常状态清单、§9 BUG 汇报；验收标准统一移至 §12 并全文重新编号（AC-1 ~ AC-42）；新增 AC-34 素材定义随导出文件走、AC-38~39 主编辑页整体、AC-40~42 异常与 BUG 汇报验收 | 课程实操要求 PRD 必含上述章节 |
| 2026-09-19 | v2.0 | 目标用户**维持「全年龄段」**（实操模板建议聚焦大学生，用户确认不改）；research.md 不动，差异记录在案 | 用户 Day 4 拍板 |
| 2026-09-19 | v2.1 | 二次自检（覆盖 v2.0 新增章节）修复 5 处「必须修改」级问题：① AC-38 漏「导入」入口，与 §4.3 顶栏布局不一致 ② E1 / AC-6 原先暗示"不支持 WebGPU = 整站不可用"，与"纯素材库也能完整玩一遍"（R2）矛盾 → 改为场景照常可用、仅生成入口受限，§1.4 平台表述同步调整 ③「复制错误详情」按钮原来没有优先级归属 → 明确为 P1-4，同步 §6.0 / §6.7 / §10.6.1 ④ §9.1 补 GitHub Issues 的渠道门槛说明与备选渠道 ⑤ §12.0 补「⏳ 标记条目在 D2 定稿前不计入验收」，让"逐条可检查"名实相符 | Day 4 复检 |
| 2026-09-19 | v2.2 | ① 新增 **V3 帮助与反馈页**（页面元素 + 交互表 + 优先级），视图总数由 2 → 3，并注明 V3 最小形态必做、扩展内容为 P1-5 ② 新增 **§6.0.1 功能对照表**（功能 → 谁用 → 哪个视图 → 动作与结果 → 验收编号），使「谁 / 在哪 / 做什么 / 看到什么」四问可直接查表 ③ 补 **AC-43**（帮助页可进可返回 + 能力边界说明可见）、**AC-44**（P1-5）——其中「能力边界说明」原先只有 §10.1 的口头承诺、没有任何验收标准 ④ P1 由 4 项增至 5 项，同步 §6.0 / §10.6.1 / §10.6.4 / §12.11 ⑤ 修正文档标题版本号（原先停留在 v2.0） | Day 4 完成后检查 |
| 2026-09-20 | v2.3 | D2 定稿（本地词典为主 + 云端 API 可选），回填 §12.5 的 AC-26，更新 §13.2 待定项 D2 状态与说明 | Day 5 技术选型（见 TECH_DESIGN.md） |
